% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Zresidual_CV.R
\name{Zresidual_CV}
\alias{Zresidual_CV}
\title{Cross-validated Z-residuals}
\usage{
Zresidual_CV(
  object,
  data,
  nfolds = NULL,
  foldlist = NULL,
  nrep = 1,
  randomized = TRUE,
  type = NULL,
  log_pointpred = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted model object. Currently supported classes are
\code{"coxph"} and \code{"survreg"}.}

\item{data}{A data frame containing the variables used to fit \code{object}.
This must be provided explicitly.}

\item{nfolds}{Number of cross-validation folds. If \code{foldlist} is not
supplied and \code{nfolds} is \code{NULL}, the default is \code{min(10, nrow(data))}.}

\item{foldlist}{Optional list of fold indices. Each element should contain
the row indices for one test fold. The folds must form a non-overlapping
partition of \code{seq_len(nrow(data_used))}, where \code{data_used} is the complete
case subset used internally.}

\item{nrep}{Number of randomized Z-residual replicates to generate for each
observation. Default is \code{1}.}

\item{randomized}{Logical. If \code{TRUE}, randomized Z-residuals are generated
for censored observations. If \code{FALSE}, only one non-randomized residual is
returned.}

\item{type}{Optional character string specifying a model subtype or residual
type. For example, \code{type = "frailty"} can be used for \code{coxph} models with
frailty terms when the frailty-specific prediction backend is required.}

\item{log_pointpred}{Optional prediction backend function. If \code{NULL}, the
backend is selected automatically by the model-specific CV function.}

\item{seed}{Optional random seed used before fold generation and randomized
residual calculation.}

\item{...}{Additional arguments passed to the model-specific CV backend and
ultimately to \code{Zresidual()}.}
}
\value{
A matrix-like object of class \code{c("cvzresid", "zresid", "matrix", "array")}, with one row per observation and one column per residual
replicate. The object contains attributes used by plotting and diagnostic
functions, including \code{foldlist}, \code{rsp}, \code{covariates}, \code{linear_pred}, and
\code{zcov}.
}
\description{
Compute cross-validated Z-residuals by dispatching to the appropriate
model-specific backend. For each cross-validation fold, the model is
refitted using the training data, and \code{Zresidual()} is then called on the
held-out test data. The fold-wise residuals are combined into one
\code{cvzresid} object.
}
\details{
This function is currently intended for models fitted with the
\code{survival} package, including \code{coxph} and \code{survreg} models. The function
does not perform Bayesian posterior summarization; it only implements
cross-validation for frequentist survival models.

\code{Zresidual_CV()} is a top-level wrapper. It does not calculate residuals
directly. Instead, it identifies the model class and calls a backend such as
\code{Zresidual_CV_survival_coxph()} or \code{Zresidual_CV_survival_survreg()}.

The intended workflow is:
\enumerate{
\item split the data into folds;
\item refit the model on the training data for each fold;
\item call \code{Zresidual()} on the held-out test data;
\item combine the fold-wise residuals into one object.
}
}
\examples{
\dontrun{
library(survival)

data(lung, package = "survival")
lung2 <- na.omit(lung[, c("time", "status", "age", "sex", "ph.ecog")])

fit <- coxph(Surv(time, status == 2) ~ age + sex + ph.ecog, data = lung2)

zcv <- Zresidual_CV(
  object = fit,
  data = lung2,
  nfolds = 5,
  nrep = 10,
  seed = 123
)

plot(zcv)
qqnorm(zcv)
}

}
