% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.zresid.R
\name{plot.zresid}
\alias{plot.zresid}
\title{Scatterplot diagnostics for Z-residuals}
\usage{
\method{plot}{zresid}(
  x,
  zcov = NULL,
  irep = 1,
  ylab = "Z-Residual",
  normality.test = c("SW", "AOV", "BL"),
  k.test = 10,
  x_axis_var = "index",
  main.title = NULL,
  outlier.return = TRUE,
  outlier.value = 3.5,
  category = NULL,
  outlier.set = list(),
  xlab = NULL,
  my.mar = c(5, 4, 4, 6) + 0.1,
  add_lowess = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric vector or matrix of Z-residuals, typically returned by
\code{Zresidual()}. Columns represent residual replications.}

\item{zcov}{Optional metadata returned by \code{Zcov()}.}

\item{irep}{Integer vector specifying which column(s) of \code{x} to plot.}

\item{ylab}{Label for the y-axis.}

\item{normality.test}{Character vector specifying which diagnostic p-values
to display. Supported values are \code{"SW"}, \code{"AOV"}, and \code{"BL"}.}

\item{k.test}{Integer controlling grouping used by the diagnostic tests.}

\item{x_axis_var}{Variable used on the x-axis. It may be \code{"index"}, \code{"lp"},
\code{"covariate"}, a covariate name in \code{zcov$covariates}, a length-n vector,
or a function \verb{function(z, zcov)} returning either a vector or a list with
components \code{values} and optionally \code{label}.}

\item{main.title}{Main title of the plot. If omitted, a default title is
constructed from the residual type metadata when available.}

\item{outlier.return}{Logical; if \code{TRUE}, mark observations with
\verb{|Z| > outlier.value} and non-finite residuals.}

\item{outlier.value}{Numeric threshold used to define outliers.}

\item{category}{Optional grouping variable of length n used to modify point
appearance.}

\item{outlier.set}{A named list of graphical arguments passed to \code{symbols()}
and \code{text()} when annotating outliers.}

\item{xlab}{Label for the x-axis. If \code{NULL}, an automatic label is used.}

\item{my.mar}{Numeric vector passed to \code{par(mar = ...)}.}

\item{add_lowess}{Logical; if \code{TRUE}, add a LOWESS smooth when the x-axis is
numeric.}

\item{...}{Additional graphical arguments passed to plotting functions.}
}
\value{
Invisibly returns a list with component \code{outliers}, containing the
indices of observations flagged as outliers for the last plotted replicate.
}
\description{
Produces scatterplots of Z-residuals against observation index, linear
predictors, model covariates, or a user-supplied x-axis variable.
}
\details{
Metadata supplied through \code{zcov} is typically returned by \code{Zcov()} and is
used to provide covariates, linear predictors, response type labels, and
plotting groups for hurdle, zero/logistic, count, and survival models.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  set.seed(1)
  n <- 30
  x <- rnorm(n)
  t_event <- rexp(n, rate = exp(0.3 * x))
  t_cens  <- rexp(n, rate = 0.4)
  status  <- as.integer(t_event <= t_cens)
  time    <- pmin(t_event, t_cens)
  dat <- data.frame(time = time, status = status, x = x)

  fit <- survival::coxph(survival::Surv(time, status) ~ x, data = dat)
  z <- Zresidual(fit, data = dat, nrep = 1, seed = 1)
  zcov <- Zcov(fit, data = dat)

  plot(z, zcov = zcov, x_axis_var = "index")
  plot(z, zcov = zcov, x_axis_var = "lp")
  plot(z, zcov = zcov, x_axis_var = "x")
}

}
\seealso{
\code{Zresidual}, \code{Zcov}
}
